export enum ENVIRONMENTS {
  PRODUCTION = 'production',
  QA = 'qa',
  STAGING = 'staging',
  DEV = 'development',
  LOCAL = 'local',
  TEST = 'test',
}

export enum PAGES {
  ROOT = '/',
  EXAMPLE = '/example',
}
